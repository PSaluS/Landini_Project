import styled from 'styled-components';

const ErrorClose = styled.div`
  font-size: 25px;
  font-weight: 700;
  position: absolute;
  top: -28px;
  right: 6px;
  cursor: pointer;
`;

export default ErrorClose;
